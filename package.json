{
  "name": "boiler-doctor-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "amplify:redirects": "node scripts/generate-amplify-redirects.mjs",
    "amplify:redirects:check": "node scripts/generate-amplify-redirects.mjs --check"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "lighthouse": "^13.4.1",
    "playwright": "^1.62.1"
  }
}
